name: Build and test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: npm ci
      - name: Debug (build)
        run: echo "Building..."
  
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Node
        uses: actions/setup-node@master
        with:
          node-version: 18
      - name: Debug (test)
        run: echo "Testing..."
